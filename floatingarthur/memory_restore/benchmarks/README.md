# 내부 모델 평가 자료

이 폴더는 시연 UI와 분리된 실험 결과만 보관한다.

- 각 실행은 시각별 하위 폴더를 생성한다.
- `benchmark_results.csv`는 후보 모델당 한 행으로 누적 가능한 표준 형식이다.
- `benchmark_report.json`은 당시의 해상도 분석·모델 선택 규칙·채택 기준을 보관한다.
- 이 폴더의 PSNR, SSIM, 처리시간은 Flask 사용자 API로 노출하지 않는다.
